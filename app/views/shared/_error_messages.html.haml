- if object.errors.any?
  .error-explanation
    .alert.alert-danger
      Your submission contains #{pluralize(object.errors.count, "error")}:
      %ul
        - object.errors.full_messages.each do |msg|
          %li= msg