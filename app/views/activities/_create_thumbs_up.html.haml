.create-thumbs-up
  = form_for(current_user.upvotes.build, remote: true) do |f|
    = hidden_field_tag :activity_id, @activity.id
    = image_tag "thumbs-up.svg"
    = f.submit t('activities.thumbs-up.submit-thumbs-up'), class: "btn btn-info"
