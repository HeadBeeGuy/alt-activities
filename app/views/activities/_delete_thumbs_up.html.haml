= form_for(current_user.upvotes.find_by(activity_id: @activity.id),
            html: { method: :delete }, remote: true) do |f|
  = hidden_field_tag :activity_id, @activity.id
  = f.submit "Un-thumbs up", class: "btn btn-secondary btn-sm"
